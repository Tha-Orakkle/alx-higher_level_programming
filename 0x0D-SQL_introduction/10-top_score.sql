-- Lists all records of the table second_table of the database in your MySQL server.
-- The records are ordered by score (descending)
SELECT `score`, `name`
FROM `second_table`
ORDER BY `score` DESC;
